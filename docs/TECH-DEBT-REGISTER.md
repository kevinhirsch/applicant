# Applicant — Tech-Debt Register

_Generated 2026-08-10 by a 13-domain parallel audit. 128 raw items._

- [ ] [UNEXPOSED][P1] Exploration-budget (explore/exploit) knob is read-only everywhere — `src/applicant/app/routers/criteria.py:155-168; a0-applicant/api/criteria.py (no action); criteria.html:148 (read-only)` — add a set_exploration_budget proxy action plus a slider/input in criteria.html's Learned Signature section (once the signature field-mismatch above is fixed).
- [ ] [UNEXPOSED][P1] Posting-alignment explainability endpoint ('why this matches your past wins') has zero UI consumer — `src/applicant/app/routers/criteria.py:126-152; a0-applicant/api/criteria.py (no action); tracker.html/digest.html (no chip)` — add an alignment proxy action and surface a 'why this matches' chip on posting cards.
- [ ] [UNEXPOSED][P1] Campaign delete/purge unreachable from the a0-applicant admin UI — `src/applicant/app/routers/campaigns.py:168; a0-applicant/api/campaigns.py (no delete branch)` — add a delete action mirroring the workspace proxy, plus a confirm-gated delete button to campaigns.html.
- [ ] [PARTIAL][P1] Onboarding wizard's Target-Roles and Key-Skills sections never populate criteria — field-name typos — `src/applicant/application/services/onboarding_service.py:417,438` — fix the two key lookups to match INTAKE_FIELD_CATALOG ('roles'/'adjacent_titles' and 'skills'); add a regression test.
- [ ] [DUP][P2] Two unsynchronized exploration_budget fields (Campaign vs LearningModel) — `core/entities/campaign.py:40; learning_service.py:194` — drop Campaign.exploration_budget or forward the campaigns PATCH into LearningService.persist_model so there is one source of truth. (reuse: LearningModel.exploration_budget via PUT /api/criteria/{id}/exploration-budget)

### Notifications & Channels
- [ ] [UNEXPOSED][P1] Web-portal presence pre-emption (FR-NOTIF-2) unreachable from a0-applicant — Discord always fires — `src/applicant/app/container.py:1043-1054; src/applicant/app/routers/digest.py:91-96; apprise_notifier.py:1210-1233` — wire a presence heartbeat from digest.html/notifications.html (mirroring the legacy workspace/js/digest.js implementation) or inject a lighter presence signal into AppriseNotifier's presence= arg; otherwise delete the machinery.
- [ ] [UNEXPOSED][P1] Realtime WebSocket 'notif' push backbone has zero consumer in a0-applicant/a0-webui — `src/applicant/app/routers/realtime.py:57-110; notification_service.py:32-67` — build a WebSocket client in a0-webui subscribing to the 'notif' channel, or add a setInterval poll to notifications.html; confirm whether the workspace front door is even deployed.
- [ ] [UNEXPOSED][P1] Notification-center panel drops deep_link/action_ref — notifications are dead-end rows — `src/applicant/app/routers/notifications.py:40-64; a0-applicant/webui/notifications.html:37-50` — add an 'Open' action calling window.openModal(n.deep_link), mirroring easy_apply.html/chat.html in the same plugin.
- [ ] [UNEXPOSED][P2] Digest force-deliver and email-preview endpoints have no proxy action or UI control — `src/applicant/app/routers/digest.py:54-71; a0-applicant/api/digest.py (get/recap/approve/decline only)` — add 'deliver' and 'email' actions plus 'Force deliver'/'Preview email' controls in digest.html.
- [ ] [PARTIAL][P2] Notifications proxy doesn't forward the engine's since/limit incremental-poll params — `a0-applicant/api/notifications.py:47-49` — extend the 'list' action to forward since/limit once any polling loop is added.
- [ ] [UNWIRED][P2] AppriseNotifier.has_discord()/has_email() are dead code — `apprise_notifier.py:384-389` — delete; configured_channels() already covers every real use case.

### Onboarding & Setup
- [ ] [PARTIAL][P0] Base-résumé wizard step overwrites the engine's own parsed intake with a client stub, breaking apply-readiness for every standard user — `a0-applicant/webui/main.html:295,135,262-269; onboarding_service.py:170-202,707-718,575-585` — skip the redundant generic save() call for the base_resume step (or make it a no-op that only advances the wizard) — do not re-POST a stub over the engine's parsed record.
- [ ] [UNEXPOSED][P0] Easy Apply consent cannot be granted from the Applicant plugin UI — `src/applicant/app/routers/setup.py:268-277; a0-applicant/api/easy_apply.py (status-only); easy_apply.html:26-70 (read-only badge)` — add a 'give_consent' proxy action forwarding to POST /api/setup/easy-apply-consent, and an 'Accept & enable Easy Apply' button — the feature otherwise 409s forever.
- [ ] [PARTIAL][P1] Wizard never rehydrates form fields from saved/resume-prefilled intake — revisits or forward-progression silently wipe data — `main.html:248-253,262-269; onboarding_service.py:789-847` — loadForm() must populate this.form from this.state.intake[section.id] before showing the section.
- [ ] [UNEXPOSED][P1] Tier-ladder's DISC-4 connection-by-reference binding has zero UI exposure — `setup_wizard.py:79-94; setup_service.py:1476-1491,1566-1567,1596-1630; tiers.html:44-61` — add a 'bind to saved connection' dropdown to each tier row, sourced from model_endpoints.html's registry.
- [ ] [UNEXPOSED][P1] POST /api/setup/llm/from-endpoint has no UI caller anywhere in the plugin — `setup.py:296-322; model_endpoints.html:214-230` — add a 'Use this model' action per model row plus a proxy action forwarding endpoint_id+model.
- [ ] [PARTIAL][P1] Automation panel dropdowns don't match backend-validated enums — `a0-applicant/webui/automation.html:139-172,191-206,208-218,233-241; setup_service.py:196-197,204-205,214,219,227,237` — resync every `<select>`'s options (and the two boolean checkboxes that should be enum selects) against the real _BROWSER_ENGINES/_BROWSER_CHANNELS/_COMPUTER_USE_BACKENDS/_COMPUTER_USE_MODES/_CAPTCHA_STRATEGIES/_EGRESS_MODES/_TAKEOVER_DESKTOPS/_RESUME_RENDER_MODES tuples; consider true partial-update semantics so one bad field doesn't block the whole save.
- [ ] [PARTIAL][P1] Sandbox-connection form omits required Proxmox credential fields — save always 422s — `a0-applicant/webui/privacy.html:70-95,184-192; setup.py:87-106` — add proxmox_token_id, proxmox_token_secret (password), clone_mode, rdp_password (password), takeover_method, takeover_url_template to the form and its POST body.
- [ ] [UNEXPOSED][P1] GET /api/setup/status's readiness/gating fields have explicit 'front door' UI intent but no renderer anywhere — `setup.py:215-248` — build the readiness card the code comments describe, reading gate_open/apply_ready/apply_missing/suggested_attributes from GET /api/setup/status.
- [ ] [DUP][P2] Engine-HTTP-forward + active-campaign-resolution boilerplate copy-pasted verbatim across 18 a0-applicant proxy files — `onboarding.py:32-75; base_resume.py:22-96 (+16 more)` — factor into one shared module, e.g. `api/_engine_client.py`. (reuse: the shared logic already implemented identically in onboarding.py/campaigns.py)
- [ ] [UNWIRED][P2] POST /api/setup/advance/{step} and the fonts_ready flag it sets are never called from any UI — `setup.py:840-853; setup_service.py:1692-1699,466-470` — wire fonts.html to call it, or remove the FONTS wizard step entirely.

### Model / LLM Routing
- [ ] [UNEXPOSED][P0] Add/Test Model Endpoint is broken end-to-end: proxy sends JSON, engine expects Form fields — `a0-applicant/api/model_endpoints.py:29-62; src/applicant/app/routers/model_endpoints.py:64-68,104-106` — change the route to accept a Pydantic body model (matching setup.py's set_tiers pattern), or send urlencoded form data from the proxy; add an integration test that exercises the real proxy dispatch without mocking _forward().
- [ ] [UNEXPOSED][P1] Model-endpoint enable/disable toggle (and generic edit) has a working route but no proxy action or UI control — `src/applicant/app/routers/model_endpoints.py:116-123; a0-applicant/api/model_endpoints.py (list/add/test/remove/models only); model_endpoints.html:73-96` — add a 'toggle'/'update' proxy action, an Enable/Disable control, and fix the status badge to read item.is_enabled not item.enabled.
- [ ] [UNEXPOSED][P1] Engine-computed endpoint health (online, ping_error, category, has_key) never rendered by the panel — `model_endpoint_service.py:147-159; model_endpoints.html:73-96` — render a status dot, inline ping_error, local/cloud badge, and key-saved indicator per row.
- [ ] [UNEXPOSED][P1] Master smart-routing flag (llm_smart_routing, default ON) has no settings-UI control; only its sub-flag is exposed — `config.py:599; automation.html:257-267` — add an llm_smart_routing checkbox alongside prefer_local; fix the stale 'default OFF' comment in container.py:826.
- [ ] [UNEXPOSED][P1] Live routing-status payload (which endpoint is actually serving, is the ladder reordered) fetched by the tiers proxy but discarded by the UI — `setup.py:325-385; tiers.html:132-147` — render res.data.routing (active endpoint, reordered flag, router.health() counts) in tiers.html.
- [ ] [PARTIAL][P1] model_type endpoint classifier captured on Add but dropped before reaching the list view, with zero downstream consumer — `model_endpoints.html:54-61,79; model_endpoint_service.py:169-238` — return model_type from list/add responses and have an adapter consult it, or delete the non-LLM dropdown options until a consumer exists.
- [ ] [PARTIAL][P2] Prefix-cache feature fully wired but inert — no registered provider profile ever enables it — `provider_profiles.py:87-97,363-390; openai_compatible.py:724-739; config.py:193` — add a cache-capable profile (e.g. Anthropic-style cache_control), or remove the PREFIX_CACHE plumbing.
- [ ] [PARTIAL][P2] ContextManager's compression lineage (traceability) computed then thrown away at its only call site — `context_manager.py:116-141; openai_compatible.py:437-451` — log/persist result.lineage, or drop the computation.
- [ ] [UNWIRED][P2] SmartLlmRouter.list_available() has zero production callers — `smart_router.py:111-126` — wire into a UI-facing routing-candidates list, or delete.
- [ ] [UNWIRED][P2] ModelEndpointService.set_enabled() has zero callers — only toggle_enabled() is used — `model_endpoint_service.py:254-259` — delete, or route an explicit-state UI control through it.
- [ ] [UNWIRED][P2] UsageLedger.peek() has no production caller — `usage_ledger.py:84-88` — surface today's not-yet-drained usage on a live cost/status endpoint, or drop it.
- [ ] [DUP][P2] ContextWindowManager duplicates ContextManager's middle-turn compression and is shadowed/inert in the production container — `context_window.py:1-148; context_manager.py; openai_compatible.py:437-451` — pass an app_context_manager into material_runner.py's build_llm() too, then delete ContextWindowManager/estimate_tokens. (reuse: applicant.application.services.context_manager.ContextManager)
- [ ] [DUP][P2] Legacy workspace/ monolith reimplements model-endpoint management and is dead weight in the current build — `workspace/routes/model_routes.py:924; ollama_routes.py` — delete if workspace/static is the only piece still consumed by the deploy, or document workspace/ as archived reference material. (reuse: model_endpoint_service.py + model_endpoints.py)

### Workspace / Companion Bridge
- [ ] [PARTIAL][P1] Research panel's cached-results view is structurally broken and swallows the panel body — `a0-applicant/webui/research.html:48,143,183-217,225-245` — pass the specific query used for the run when checking cache (mirror workspace's applicantDigest.js cached/run pattern) instead of calling cached with no query; stop hiding the whole panel behind fatalError from one failed request.
- [ ] [PARTIAL][P1] Interview-prep brief is generated but every displayed field is blank — key-name mismatch — `a0-applicant/webui/interview_prep.html:69-92; material_service.py:1676-1682` — rename engine keys to match the UI contract (company→company_name, role→role_title, notes→talking_points, key_requirements→key_skills).
- [ ] [DUP][P1] Internal-bridge calendar query reimplements list_events without recurrence expansion, silently hiding recurring interviews from auto-detection — `workspace/routes/applicant_internal_routes.py:355-393` — factor list_events's filter+expand logic into a shared helper instead of the simplified duplicate. (reuse: calendar_routes.py list_events (636-696) + _expand_rrule (414))
- [ ] [PARTIAL][P1] a0-applicant Mind panel is a stripped-down reimplementation missing deny + ACE playbooks that the native Mind modal already proves out — `a0-applicant/api/mind.py:42-76; mind.html:90-110` — port the deny button + ACE-playbook viewer/editor from workspace's applicantMind.js so the FR-MIND-9 review-before-write safety mechanism is usable from this surface.
- [ ] [UNEXPOSED][P1] Vault panel proxies bank-account status and master-key rotation but exposes no control to reach them — `a0-applicant/api/vault.py:63-74; vault.html` — add an 'Account' section and a 'Rotate master key' control, or remove the dead dispatch branches.
- [ ] [PARTIAL][P2] Mind panel's memory-kind badge never matches, always rendering the wrong color — `mind.html:55` — fix the comparison to `item.kind === 'environment'`.
- [ ] [PARTIAL][P2] Skill PUT ('full rewrite') silently behaves like PATCH (partial merge) on the companion side — `applicant_internal_routes.py:1123-1136,669-681; skills.py:366-391` — implement a true replace path for PUT, or fix the docstring to say it's a merge.

### Privacy / EEO / Attributes / Automation
- [ ] [UNEXPOSED][P1] PII retention window is settable in UI but never actually enforced — `config.py:216-218; setup.py:112-138 (no field); admin.py:527-557 (no UI caller)` — add a persisted pii_retention_schedule off/daily control next to pii_retention_days, and a 'Run retention sweep now' button wired to the existing admin endpoint.
- [ ] [PARTIAL][P1] Attributes proxy drops is_integral/confirm — FR-FB-3 protected-attribute gate unreachable from the shipped UI — `a0-applicant/api/attributes.py:79-86; attributes.html:37-42` — port the is_integral checkbox + 409-confirm-retry logic from the legacy frontend/static/applicant/js/attributes.js.
- [ ] [UNEXPOSED][P2] Field-mapping binding (FR-ATTR-2) has no proxy action or manual-override UI — `src/applicant/app/routers/attributes.py:141-158` — add a manual mapping-override UI, or delete the endpoint if mapping is meant to be purely auto-learned (see the related wiring gap under Scoring & Learning).
- [ ] [DUP][P2] Two divergent Attributes UI implementations ship side by side — `frontend/static/applicant/attributes.html vs a0-applicant/webui/attributes.html` — fold the legacy UI's is_integral/confirm handling into the plugin panel and retire the legacy bare routes. (reuse: a0-applicant/webui/attributes.html)
- [ ] [UNWIRED][P2] AttributeCloudService.resolve_missing() is dead code, duplicated by the real production path — `attribute_cloud_service.py:193-214` — delete resolve_missing() and MissingAttributeError. (reuse: PrefillService._block_missing_attr / _emit_waiting)
- [ ] [PARTIAL][P2] Automation panel silently drops the captcha-key-configured status flag — `automation.html:251-254` — render captcha_api_key_configured as a 'key already saved' label next to the input.

### UI Panels Reachability
- [ ] [UNEXPOSED][P0] Stuck/blocked-application recovery (retry, override) and evidence screenshots are entirely unreachable — `src/applicant/app/routers/admin.py:115,216,233,253,271 (+resume-status, research-provenance, learning, lessons, stealth, workspace-bridge, captcha-status, capacity, embedding-backend, retention/prune); a0-applicant/api/ops.py:74-97 (allowlists only 5 of 27 admin.py routes)` — extend ops.py's action allowlist with stuck_applications/retry_stuck/blocked_applications/override_blocked/screenshots (and the rest), and add a 'Stuck & Blocked' section to ops.html or debug.html; this subsumes the narrower blocked-applications, embedding-backend, and retention-prune findings elsewhere in this register.
- [ ] [PARTIAL][P1] Résumé-upload conflict resolution is display-only — main.html never calls the engine's confirm-conflict endpoint — `a0-applicant/webui/main.html:121-128; src/applicant/app/routers/onboarding.py:337; a0-applicant/api/onboarding.py:78-97` — add a confirm_conflict proxy action and per-field accept/keep-mine buttons in main.html's conflicts block.
- [ ] [PARTIAL][P1] Vault 'Delete credential' button calls a DELETE route the engine never implemented — `a0-applicant/webui/vault.html:75,181-186; a0-applicant/api/vault.py:59-61; src/applicant/app/routers/credentials.py (no DELETE route)` — add DELETE /{campaign_id}/{tenant_key} to credentials.py (storage-layer delete almost certainly already exists).
- [ ] [DUP][P2] a0-applicant/api/features.py duplicates dormant.py's engine call to compute a nav-gating payload nothing consumes — `a0-applicant/api/features.py:227-291` — delete unless a concrete nav-graying consumer is scoped; if kept, wire it and drop it from the BACKEND_ONLY test allowlist. (reuse: a0-applicant/api/dormant.py)
- [ ] [UNEXPOSED][P2] Purpose-built agent live-status endpoint (/api/agent/status) has zero callers anywhere in the plugin — `src/applicant/app/routers/agent_status.py:34-37,124` — proxy it from api/agent_runs.py (or today.html/activity.html) as the panels' primary 'what's happening now' source.
- [ ] [UNWIRED][P2] HelloWorld scaffold API handler has no caller anywhere and is permanently allowlisted as dead — `a0-applicant/api/hello.py:5-12` — delete it and drop 'hello' from the BACKEND_ONLY set in tests/unit/test_release_readiness.py.

## Suggested resolution order

**Phase 0 — stop the bleeding (P0s, this week).** These either corrupt data, submit the wrong thing, or block a consent/safety gate:
1. Tailored résumé never rendered/uploaded (Drafting 3.1) + tailoring_for() dead dispatch (3.2) — fix together, same subsystem.
2. force_status_checked not wired (Applications 5.1) and Live-Takeover wrong endpoint (5.2) — both are "the safety check exists, the real call site bypasses it."
3. Add/Test Model Endpoint JSON/Form mismatch (Model/LLM 10.1) — one-line-class fix, unblocks all model-endpoint work below it.
4. Base-résumé wizard overwrite bug (Onboarding 9.1) — fix before touching anything else in main.html.
5. Expose the four P0 review/consent gates that currently have zero UI: résumé-variant approval (Drafting 3.4), criteria editable UI (Criteria 7.1), Easy Apply consent (Onboarding 9.7), missing-attribute resume loop (Scoring 2.1).
6. Expose the two P0 pipeline-visibility gaps: post-submission outcome tracking (Tracker 6.3), cover-letter/flagged-facts/JD-match (Drafting 3.5 + 3.9).
7. Widen the ops.py admin allowlist for stuck/blocked applications + screenshots (UI Panels).

**Phase 1 — cheap high-value exposes (P1 UNEXPOSED, ~1-2 days each, no dependencies).** Batch by panel since most are "add one proxy action + one control":
- criteria.html / campaigns.html: exploration-budget, posting-alignment, run-mode/throughput, cost guardrails, campaign delete.
- documents.html / conversion.html: aggressiveness dial, screening-answer reuse, conversion PDF view.
- model_endpoints.html / tiers.html: enable/disable, health fields, smart-routing flag, routing-status payload, from-endpoint binding, tier connection-binding.
- digest.html / notifications.html: presence heartbeat, realtime consumer (or fallback poll), notification deep-links.
- tracker.html: presubmit-safety warnings, blocked-applications section, manual "I submitted" fallback.
- vault.html: bank-account status + rotate-key controls.

**Phase 2 — dedup before more code piles on top of the wrong implementation.** Do these before Phase 1's panels get more logic added to their proxies:
- Build the shared `api/_engine_client.py` and migrate the 18 duplicated proxy files (Onboarding 9.9) — do this first since every other proxy edit in Phase 1 touches these files.
- Wire RejectionService and SilenceService into their real call sites (Tracker 6.9, 6.10).
- Merge ContextWindowManager into ContextManager (Model/LLM 10.12).
- Merge SubmissionService's inline snapshot logic into SubmissionSnapshotService (Applications 5.6).
- Fix the internal-bridge calendar query to reuse list_events + _expand_rrule (Workspace 11.3) — this one silently drops recurring interviews, treat as correctness not just cleanup.
- Consolidate the `_esc` helper (Drafting 3.11) and the two Attributes UIs (Privacy 12.7).

**Phase 3 — mechanical fixes: field-mismatches and swallowed errors (batch these, they're nearly all one-line diffs).**
- Response-shape mismatches: discovery.html (1.1), criteria.html signature (2.2), tracker.html board + attention (6.1, 6.2), audit.html (6.6), digest.html weekly recap (4.3), feedback.html criteria-delta (4.4), interview_prep.html (11.2), mind.html badge (11.6).
- Silent-failure swallowing: digest.html approve/decline (4.5), today.html resolve/snooze (4.6).
- Config/enum drift: automation.html dropdowns (9.5), privacy.html sandbox form (9.6).
- Broken deep-links and dead buttons: digest Review link (4.2), vault delete route (13-item under UI Panels), onboarding conflict-resolution (13-item under UI Panels).

**Phase 4 — wire the remaining UNWIRED P1/P2 items** (field-mapping-binding into prefill, neko-rooms control plane, advance_to_awaiting_response into the sweep, OutcomeRecorded subscriber) once Phase 2's canonical implementations exist to wire into.

**Phase 5 — delete confirmed dead code** (cleanup sprint, no user-facing risk): learning_flywheel.py, ai-add endpoint, agent_question(), render_webpage, pending-count action, legacy digest frontend, legacy workspace/model_routes.py, NoVncRemoteView, has_discord/has_email, SmartLlmRouter.list_available, ModelEndpointService.set_enabled, UsageLedger.peek, capability_registry outcomes.*, HelloWorld scaffold, resolve_missing(), features.py, advance/{step} wizard endpoint.