# Exhaustive Audit 2 — Lens 08: Dark Engine Capability (the systematic BE→FE matrix)

**Lens.** The prior audit's #1 finding was "built but dark." This pass makes that sweep systematic and
exhaustive: every engine router endpoint traced through workspace proxy → JS consumer → nav/feature
registry; every application-service public method with no router; every entity field computed but never
rendered; every config knob with no Settings UI; every adapter wired in `container.py` but unreachable;
every scheduler/tick behavior invisible to the user.

**Method.** Grounded greps of `src/applicant/app/routers/*.py` (31 routers, ~140 endpoints),
`workspace/src/applicant_engine.py` (client), the 23 `workspace/routes/applicant_*_routes.py` proxy
files, all `workspace/static/js/applicant*.js` consumers, `workspace/src/applicant_features.py`
(section registry), `src/applicant/application/services/*.py` (50 services),
`src/applicant/core/entities/*.py` (27 entities), `src/applicant/app/config.py` (~75 knobs),
`src/applicant/app/container.py`, and `scheduler.py`/`agent_loop.py`.

**Dedup.** Per the brief, these Wave 1–2 items are NOT repeated: learning summary
funnel/per-source/signature (Results surface), per-campaign pause/resume (global pause shipped),
submission-snapshot pre-submit preview, activity stats/recap/momentum. The post-submission
tracker/follow-ups/ghosting headline is taken a level deeper here (exact methods, fields, and wiring
chains) rather than restated.

---

## A. The router → proxy → JS → nav matrix (summary)

Chain legend: **E** engine endpoint · **P** workspace proxy · **J** JS consumer · **N** nav/section
(`applicant_features.py` or unconditional surface). ✔ wired · ✖ missing · ◐ partial/admin-only.

| Engine router (prefix) | E | P | J | N | Verdict |
|---|---|---|---|---|---|
| `setup.py` (`/api/setup/*`) | ✔ | ✔ `applicant_setup_routes` | ✔ `applicantOnboarding.js` | ✔ wizard/Settings | Wired |
| `model_endpoints.py` | ✔ | ◐ GET/POST/test/models only | ✔ onboarding | ✔ | **PATCH/DELETE dark** (§B3) |
| `campaigns.py` | ✔ | ◐ GET/POST/PATCH only | ✔ | ✔ switcher | **DELETE dark** (§B3) |
| `agent_runs.py` + `agent_status.py` | ✔ | ✔ `applicant_ops/activity` | ✔ Debug/Activity/CampaignSettings | ✔ | Wired; scheduler health part-rendered (§B7) |
| `discovery_sources.py` | ✔ | ✔ ops `/discovery` | ✔ CampaignSettings | ✔ | Wired |
| `criteria.py` | ✔ | ✔ memory routes | ✔ entities.js | ✔ memory | Wired |
| `digest.py` | ✔ | ✔ email routes | ✔ `applicantDigest.js` | ✔ email | Wired (one dead dup lane, §B1) |
| `documents.py` | ✔ | ◐ | ✔ documentLibrary | ✔ documents | **variant PDF download, redline, deferred-essay dark**; ensure-submittable JS broken (§B1/§B3) |
| `conversion.py` | ✔ | ◐ | ✔ onboarding | ✔ | **preview PDF download dark** (§B3) |
| `onboarding.py` | ✔ | ✔ setup routes | ✔ | ✔ wizard | Wired |
| `attributes.py` | ✔ | ✔ memory routes | ◐ | ✔ memory | `acquire-missing` proxy dead (§B1) |
| `pending_actions.py` | ✔ | ✔ portal + chat | ✔ Portal | ✔ | Wired |
| `notifications.py` | ✔ | ✔ portal | ✔ Portal | ✔ | Wired |
| `chat.py` | ✔ | ✔ chat routes | ✔ applicantChat | ✔ chat | Wired; 3 app-action proxies unused (§B1) |
| `outcomes.py` | ◐ | ◐ admin-gated only | ◐ Debug only | ✖ owner | **Whole owner lane dark** (§B2) |
| `admin.py` (history/screenshots/workflow/logs/variants/learning/tools) | ✔ | ✔ admin routes (admin-gated) | ◐ Debug modal | ◐ debug section | **Owner value locked behind admin** (§B4) |
| `admin.py` detections + stealth | ✔ | ✔ `:198`/`:265` | ✖ zero JS | ✖ | **Half-wired dark** (§B4) |
| `audit.py` exports | ✔ | ✔ admin-gated | ◐ campaign-level only | ◐ | App-level export dark (§B4) |
| `credentials.py` | ✔ | ◐ vault routes | ✔ applicantVault | ✔ vault | **rotate-key dark** (§B3) |
| `remote.py` | ✔ | ✔ remote routes | ✔ applicantRemote | ✔ takeover/desktop | Wired |
| `compare.py`, `gallery.py`, `feedback.py`, `fonts.py`, `research.py`, `update.py`, `agent_memory.py` | ✔ | ✔ | ✔ | ✔ | Wired (feedback read-back dark, §B3) |
| `mcp.py` (`/mcp/*`) | ✔ | ✖ | ✖ | ✖ | Dark by design — undocumented (§B3) |
| **No router at all**: post-submission tracker, follow-ups, ghosting, rejection scan, playbooks, flywheel, retention/erasure-on-demand, capacity, presubmit-skip reasons | ✖ | ✖ | ✖ | ✖ | **The mother lode** (§B2, §B7) |

---

## B. Ranked findings

### B1 — Broken or dead wiring found during the trace (fix first; minutes each)

1. **`applicantReachability.js` is a facade over three 404s** — [VALUE: high · EFFORT: S] — the module is loaded on every page (`workspace/static/index.html:2624`) and exports `ensureSubmittable` → `POST /api/applicant/documents/ensure-submittable` (real proxy is `/api/applicant/documents/applications/{application_id}/ensure-submittable`, `applicant_documents_routes.py:334`), `fetchLearnedCriteria` → `GET /api/applicant/criteria/{cid}/learned` (no `/api/applicant/criteria` router exists — live path is under `/api/applicant/memory`), and `deliverDigestNow` → `POST /api/applicant/digest/deliver-now` (no such route; live paths are `applicant_email_routes.py:291` and `applicant_portal_routes.py:393`). All three URLs 404, and no JS module imports any of the three exports — the "reachability wiring" file is itself unreachable AND wrong. Anchor: workspace/static/js/applicantReachability.js:9-21.
2. **`ensure-submittable` (auto-heal missing materials) has no working caller** — [VALUE: high · EFFORT: S] — engine `POST /api/documents/applications/{id}/ensure-submittable` (src/applicant/app/routers/documents.py:419) rebuilds whatever material an application is missing; proxy exists (`applicant_documents_routes.py:334`); the only JS reference is the broken helper in item 1 → a one-click "fix this application's documents" action the review surface never offers. Wire a real button in `documentLibrary.js` review cards + Portal blocked-material rows. Anchor: src/applicant/app/routers/documents.py:419.
3. **Chat-lane application actions are dead proxies** — [VALUE: low · EFFORT: S] — `applicant_chat_routes.py:395/403/411` proxy request-final-approval / resume-account-step / resume-detection-step for the chat surface, but `applicantChat.js` only calls `campaigns/confirm/confirm-criteria/message` (+ pending-actions); the remote lane owns these actions. Either wire chat action chips to them or delete the routes. Anchor: workspace/routes/applicant_chat_routes.py:395.
4. **`memory/attributes/acquire-missing` proxy is dead code** — [VALUE: low · EFFORT: S] — `applicant_memory_routes.py:302` proxies the engine's acquire-missing (`attributes.py:161`), but zero JS calls it — the Portal's own `/missing-attribute` route (`applicant_portal_routes.py:431`) is the live lane. Consolidate or delete. Anchor: workspace/routes/applicant_memory_routes.py:302.
5. **Duplicate digest-deliver lane, one side unused** — [VALUE: low · EFFORT: S] — `applicant_email_routes.py:210` (`POST /digest/{cid}/deliver`) has no JS caller; `applicant_email_routes.py:291` (`/campaigns/{cid}/digest/deliver`, used by `emailLibrary/applicantDigest.js:982`) is the live one. Remove the dup before someone "fixes" the wrong lane. Anchor: workspace/routes/applicant_email_routes.py:210.

### B2 — The post-submission lifecycle, a level deeper (no engine router exists for ANY of this)

The prior audit said "the tracker has no surface." Here is exactly what is built and exactly what wiring is missing. The subsystem is **triple-dark**: the DB tables exist (migration `0009_g16_post_submission` created `follow_ups`, `ghosting_signals`, `rejection_signals`), the container builds `PostSubmissionService` (src/applicant/app/container.py:868) and injects it into `SubmissionService` as `self._post_submission` (src/applicant/application/services/submission_service.py:139) — and **zero call sites exist on that field, no router imports any of its eleven public methods, and no scheduler tick drives it**.

6. **Tracker read-model: `poll_status()` per application** — [VALUE: high · EFFORT: M] — `PostSubmissionService.poll_status(application_id)` (src/applicant/application/services/post_submission_service.py:51) returns the live tracker state (`submitted → awaiting-response → following-up → rejected/ghosted → archived`); `GET /api/outcomes` is a bare index stub (`{"surface": "outcomes"}`, src/applicant/app/routers/outcomes.py:38). Wiring: replace the stub with a campaign-scoped list (`GET /api/outcomes/{campaign_id}` → per-application `{status, last_event, days_since_submission}`), add owner proxy `workspace/routes/applicant_tracker_routes.py`, render an applied→interview→offer board (nav next to Results). Anchor: src/applicant/application/services/post_submission_service.py:51.
7. **Follow-up scheduling + the send queue** — [VALUE: high · EFFORT: M] — `schedule_follow_up(application_id, template, delay_hours, subject, body)` (post_submission_service.py:149) and `send_scheduled_follow_ups(now)` (post_submission_service.py:169) implement a full timed follow-up queue over the `FollowUp` entity (`status SCHEDULED/SENT`, `scheduled_at`, `sent_at`, `subject`, `body` — src/applicant/core/entities/follow_up.py:23-31) — no router, no proxy, no JS; the user can never see "follow-up to Acme drafts Friday," approve it, or cancel it. Wiring: `GET/POST /api/follow-ups/{campaign_id}` + `POST /api/follow-ups/{id}/cancel` → owner proxy → a Follow-ups tab on the tracker board + Portal rows for due drafts. Anchor: src/applicant/application/services/post_submission_service.py:149.
8. **Follow-up drafting (`FollowUpService.draft_followup`)** — [VALUE: high · EFFORT: M] — `draft_followup(...)` (src/applicant/application/services/followup_service.py:48) plus `followup_is_due(...)`:36 and the `due_after_days` policy:31 generate the message body itself — the emotional "it nudged Acme for me" beat. Dark end-to-end; the review-before-send loop should reuse the existing document review/turn UI (`documentLibrary.js`) per lift-and-shift. Anchor: src/applicant/application/services/followup_service.py:48.
9. **Ghosting detection (`check_ghosting` + `SilenceService`)** — [VALUE: high · EFFORT: M] — `check_ghosting(campaign_id, sla_days=14)` (post_submission_service.py:115) walks submissions and emits `GhostingSignal(sla_days, submission_age_days, detail)` (src/applicant/core/entities/ghosting_signal.py:13-14) via `SilenceService.is_likely_ghosted` (silence_service.py:53) — no router, no scheduler tick, and the 14-day SLA is a hardcoded call-site default with no Settings knob. Also latently broken: `_submission_age` (post_submission_service.py:143) returns `timedelta(0)` for submitted apps, so `age.days` can never cross the SLA — fix alongside the wiring. Wiring: a daily scheduler step (like the retention sweep at scheduler.py:623) → notification + tracker "ghosted" column; SLA slider in Settings. Anchor: src/applicant/application/services/post_submission_service.py:115.
10. **Rejection email scanning never meets the inbox the workspace already has** — [VALUE: high · EFFORT: M] — `scan_email_for_rejection(email_subject, email_body, application_id)` (post_submission_service.py:92) + `RejectionService.classify_message`/`classify_status_page` (src/applicant/application/services/rejection_service.py:66,78) are built, and the front-door already polls mail (`workspace/routes/email_pollers.py`, `emailInbox.js`) — the two are never connected. (Note the duplication: `scan_email_for_rejection` reimplements its own weaker keyword list instead of reusing `RejectionService`'s 15 high-precision phrase markers — unify when wiring.) Wiring: `POST /api/outcomes/applications/{id}/scan-email` (or an internal-token lane the workspace poller calls) → outcomes recorded → tracker/Portal badge. Anchor: src/applicant/application/services/post_submission_service.py:92.
11. **Manual "they rejected me" capture** — [VALUE: high · EFFORT: S] — `process_rejection_signal(application_id, source, signal_text, confidence, detail)` (post_submission_service.py:76) transitions state + persists a `RejectionSignal` (`signal_text`, `confidence` — src/applicant/core/entities/rejection_signal.py:20-21) — no router; the learning loop's strongest negative signal depends on the user being able to report it in one tap. Wiring: `POST /api/outcomes/applications/{id}/rejection` → tracker row button. Anchor: src/applicant/application/services/post_submission_service.py:76.
12. **`enter_post_submission` / `advance_to_awaiting_response` transitions never fire from any surface** — [VALUE: med · EFFORT: S] — post_submission_service.py:31 and :46 are the entry points of the state machine; nothing user-reachable invokes them, so even a submitted application never *enters* the tracker. Wire them into the existing final-submit confirmations (`remote.py:292/323` handlers). Anchor: src/applicant/application/services/post_submission_service.py:31.
13. **`archive()` — no way to close out an application** — [VALUE: med · EFFORT: S] — post_submission_service.py:213 archives an application; no router/proxy/JS → dead rows accumulate forever in every future tracker view. Wiring: `POST /api/outcomes/applications/{id}/archive` → swipe/button on tracker rows. Anchor: src/applicant/application/services/post_submission_service.py:213.
14. **One-tap outcome capture is admin-only** — [VALUE: high · EFFORT: S] — `POST /api/outcomes/applications/{id}/mark-submitted` (src/applicant/app/routers/outcomes.py:74) and `/detect` (:43) exist on the engine but are proxied ONLY behind `_require_admin` (`applicant_admin_routes.py:333`, `:352`) and consumed only by the Debug modal (`applicantDebug.js`). The owner — the person who knows they got the interview — has no owner-scoped lane. Wiring: add to the tracker proxy, buttons on tracker/Portal rows. Anchor: workspace/routes/applicant_admin_routes.py:333.
15. **Per-application outcome timeline is Debug-only** — [VALUE: med · EFFORT: S] — `GET /api/outcomes/applications/{id}/log` (src/applicant/app/routers/outcomes.py:93) is proxied admin-only (`applicant_admin_routes.py:272`) and rendered as a comma list in `applicantDebug.js:357`; `OutcomeEvent.type/source` (src/applicant/core/entities/outcome_event.py:46-47) deserve a real owner-visible timeline on each tracker row. Anchor: src/applicant/app/routers/outcomes.py:93.

### B3 — Engine endpoints with NO workspace proxy at all

16. **The tailored resume PDF cannot be downloaded** — [VALUE: high · EFFORT: S] — `GET /api/documents/variants/{variant_id}/download` (src/applicant/app/routers/documents.py:134) serves the compiled PDF artifact; `applicant_engine.py` has no method for it, no proxy, no JS → the user can approve a resume they can never save/open. Wiring: client method → streaming proxy in `applicant_documents_routes.py` → download icon on `documentLibrary.js` variant cards and Debug variants tab. Anchor: src/applicant/app/routers/documents.py:134.
17. **Campaign deletion is impossible from the product** — [VALUE: high · EFFORT: S] — `DELETE /api/campaigns/{campaign_id}` (src/applicant/app/routers/campaigns.py:95) exists and correctly routes through `DataLifecycleService` + `ErasureService.delete_campaign` (src/applicant/application/services/erasure_service.py:22) for a clean purge — no client method, no proxy, no UI. Users can create job searches forever and never remove one (a privacy promise the engine already keeps). Wiring: client → owner proxy with confirm → Campaign Settings danger zone. Anchor: src/applicant/app/routers/campaigns.py:95.
18. **Vault master-key rotation unreachable** — [VALUE: med · EFFORT: S] — `POST /api/credentials/rotate-key` (src/applicant/app/routers/credentials.py:127) re-encrypts every secret under a fresh key; no proxy in `applicant_vault_routes.py` (which stops at store/capture/account/tenants) → the flagship vault-hygiene action is env/curl-only. Wiring: vault proxy + "Rotate encryption key" button in `applicantVault.js`. Anchor: src/applicant/app/routers/credentials.py:127.
19. **Conversion preview PDF download dark** — [VALUE: med · EFFORT: S] — `GET /api/conversion/{campaign_id}/preview/download` (src/applicant/app/routers/conversion.py:72) serves the compiled LaTeX preview the user is asked to accept/reject; the accept decision (`applicantOnboarding.js`) is made without being able to open the actual PDF. Wiring: client + setup-routes proxy + "Open preview PDF" link in the conversion step. Anchor: src/applicant/app/routers/conversion.py:72.
20. **Engine model endpoints can be added but never edited or removed** — [VALUE: med · EFFORT: S] — engine `PATCH /api/model-endpoints/{id}` (src/applicant/app/routers/model_endpoints.py:116) and `DELETE` (:126) have no proxy — `applicant_setup_routes.py:274-327` covers only list/add/test/models — so stale or mistyped endpoints accumulate forever in the ladder. Wiring: two thin proxies + edit/delete affordances in the existing endpoint manager. Anchor: src/applicant/app/routers/model_endpoints.py:116.
21. **Deferred long-essay generation dark** — [VALUE: med · EFFORT: M] — `POST /api/documents/deferred-essay` (src/applicant/app/routers/documents.py:265) generates the "come back to this essay question later" answer; no client/proxy/JS — the pre-fill stop-boundary can park an essay, but the user has no surface to trigger/review the deferred draft. Wiring: proxy + a Portal pending-action of kind essay → documentLibrary review. Anchor: src/applicant/app/routers/documents.py:265.
22. **Standalone redline render endpoint unproxied** — [VALUE: low · EFFORT: S] — `POST /api/documents/redline` (src/applicant/app/routers/documents.py:204) renders an arbitrary base-vs-new redline; the UI reaches redlines only via `/{id}/review` session state. Fine day-to-day, but the "diff any two versions" capability is dark (would power a variant-lineage diff view, see item 47). Anchor: src/applicant/app/routers/documents.py:204.
23. **Feedback is write-only — the user can never see what they taught it** — [VALUE: med · EFFORT: M] — `GET /api/feedback` is a stub index (src/applicant/app/routers/feedback.py:32) while `FeedbackSummaryProvider` (src/applicant/application/services/feedback_history.py:50) already walks per-application feedback for the curation nudge — the read-model exists, no endpoint returns it. Wiring: real `GET /api/feedback/{campaign_id}` from the provider → memory/Mind proxy → "What you've told it" list under the Mind panel. Anchor: src/applicant/application/services/feedback_history.py:50.
24. **MCP tool surface is entirely undocumented product capability** — [VALUE: low · EFFORT: M] — `/mcp/tools` list + `/mcp/tools/call` (src/applicant/app/routers/mcp.py:56,76) expose the engine to external agents, but the `api` service is internal-only and nothing in the front-door or docs mentions it — a differentiator (drive Applicant from your own agent) that no user can discover or reach. Wiring: token-gated passthrough or at minimum a Settings > Integrations card documenting it. Anchor: src/applicant/app/routers/mcp.py:56.

### B4 — Admin-gated or JS-orphaned: proxied, but the owner never sees it

25. **The application history list itself is admin-only** — [VALUE: high · EFFORT: M] — `GET /api/admin/history/{campaign_id}` (src/applicant/app/routers/admin.py:59) already returns exactly the tracker-board payload (per-application `status`, `work_mode`, screenshots count, `outcomes[]` — rendered only inside `applicantDebug.js:295-309`); the owner-facing Results surface shows aggregates with no drill-down list. Promoting this one endpoint to an owner proxy delivers 80% of item 6 immediately. Anchor: src/applicant/app/routers/admin.py:59.
26. **Detection events: proxied, zero JS anywhere** — [VALUE: med · EFFORT: S] — `GET /api/admin/detections/{campaign_id}` (src/applicant/app/routers/admin.py:88) surfaces `DetectionEvent.signal_type/detail` (src/applicant/core/entities/detection_event.py:17-18) — bot-checks/blocks the agent hit; the proxy exists (`applicant_admin_routes.py:198`) but grep finds NO JS consumer, not even a Debug tab. The user never learns "this site is blocking automation, expect handoffs." Wiring: Debug tab first, then a plain-language warning chip on affected tracker rows. Anchor: workspace/routes/applicant_admin_routes.py:198.
27. **Stealth posture panel: proxied, zero JS** — [VALUE: med · EFFORT: S] — `GET /api/admin/stealth` (src/applicant/app/routers/admin.py:152) reports the full stealth state (engine, persona, egress); `applicant_admin_routes.py:265` proxies it but no JS reads it — only the one-line `/caveat` lane (`applicant_remote_routes.py:341`) is consumed. A "how am I presenting to job sites" panel (browser engine, timezone, proxy mode) belongs in Settings > Automation, especially given item 39. Anchor: workspace/routes/applicant_admin_routes.py:265.
28. **Application screenshots locked in Debug** — [VALUE: med · EFFORT: S] — `GET /api/admin/screenshots/{application_id}` (src/applicant/app/routers/admin.py:107) returns per-page proof-of-work captures (`applicant_admin_routes.py:218`, rendered only as labels in `applicantDebug.js:352`); showing the actual images on review/tracker rows is the cheapest trust artifact the product has. Anchor: src/applicant/app/routers/admin.py:107.
29. **Durable workflow state ("where is it stuck") admin-only** — [VALUE: med · EFFORT: S] — `GET /api/admin/workflow/{application_id}` (src/applicant/app/routers/admin.py:97) exposes step progression + `pending_recovery` (`applicantDebug.js:355`); an owner whose application sits blocked for days can see nothing. Surface as a step chip on Portal/tracker rows. Anchor: src/applicant/app/routers/admin.py:97.
30. **Application-level audit export proxied but unreachable** — [VALUE: low · EFFORT: S] — `GET /api/audit/application/{id}/export.json` (src/applicant/app/routers/audit.py:74) is proxied (`applicant_admin_routes.py:315`) but the Debug UI only wires the campaign-level export (`applicantDebug.js:272`); the per-application "exactly what happened" download — the natural attachment to a dispute or a bug report — has no button. Anchor: workspace/routes/applicant_admin_routes.py:315.
31. **Ordered campaign audit log export is admin-only** — [VALUE: med · EFFORT: S] — `audit.py:59` + `applicant_admin_routes.py:299` are gated `_require_admin`; the honesty artifact the single-user *owner* needs when deciding to trust the agent requires operator rank. Re-scope to owner (all data is theirs). Anchor: workspace/routes/applicant_admin_routes.py:299.
32. **Redacted run logs admin-only, unlinked from failures** — [VALUE: low · EFFORT: S] — `GET /api/admin/logs` (src/applicant/app/routers/admin.py:119) renders in a Debug tab, but nothing deep-links a failed Portal item to its log slice; an "agent had trouble → view what happened" link would reuse it as-is. Anchor: src/applicant/app/routers/admin.py:119.

### B5 — Application-service capability with no router (beyond the tracker)

33. **Promote a winning variant to base resume** — [VALUE: high · EFFORT: S] — dark chain: `MaterialService.promote_to_base_resume(variant)` (clears `parent_id`, marks approved, so future tailoring forks from the user's best variant; idempotent) → zero callers, no router (documents.py has the variant library but no promote) → `POST /api/documents/variants/{id}/promote` → `applicant_documents_routes.py` → a Promote button on `documentLibrary.js` variant cards. Anchor: src/applicant/application/services/material_service.py:672.
34. **Pre-fill silent-degradation diagnostics ring is write-only memory** — [VALUE: high · EFFORT: S] — dark chain: `PrefillService.diagnostics()` is a bounded, deduped ring of credential/LLM/login failure messages built explicitly "so it is surfaced rather than lost" → no router reads it → add beside `workflow_state` in admin.py → admin proxy → a Diagnostics strip in `applicantDebug.js` (and a plain-language Portal hint when non-empty). Anchor: src/applicant/application/services/prefill_service.py:244.
35. **Emergency copy/paste handoff is dead code** — [VALUE: high · EFFORT: M] — dark chain: `PrefillService.emergency_handoff(application, attributes)` assembles the would-have-been-filled values for manual paste, lands EMERGENCY_DATA_HANDOFF, and emits a pending action — the spec'd last-resort fallback — but neither the loop's fill-failure path nor any router ever invokes it → call from the prefill failure branch + `POST /api/remote/applications/{id}/emergency-handoff` → remote proxy → `applicantRemote.js`. Anchor: src/applicant/application/services/prefill_service.py:510.
36. **Clone campaign** — [VALUE: med · EFFORT: S] — dark chain: `CampaignService.clone_campaign(source_id, name)` duplicates a campaign (criteria, settings) → zero callers, no router → `POST /api/campaigns/{id}/clone` → campaigns proxy → a Duplicate action in `applicantCampaignSettings.js` (the natural "same search, new city" move). Anchor: src/applicant/application/services/campaign_service.py:96.
37. **PII retention sweep: no manual trigger, no report** — [VALUE: med · EFFORT: S] — dark chain: `DataLifecycleService.prune_pii_older_than(days)` returns per-store pruned counts but is called only from the scheduler tick (scheduler.py:623), which itself is dormant (schedule defaults "off", §B8 item 87) → the user cannot run a sweep on demand, preview it, or see "last sweep removed N records" → `POST /api/admin/retention/prune` + last-result GET → ops proxy → Settings privacy panel. Anchor: src/applicant/application/services/data_lifecycle_service.py:110.
38. **Cached company-research reports unretrievable** — [VALUE: med · EFFORT: S] — dark chain: `ResearchService.cached_report(campaign_id, query)` returns an already-paid-for report without burning call budget; research.py only exposes `run` (which re-runs) → `GET /api/research/{campaign_id}/cached` → research proxy → show cached findings on results/review surfaces. Anchor: src/applicant/application/services/research_service.py:101.
39. **Per-posting "match to your past wins" explainability** — [VALUE: med · EFFORT: S] — dark chain: `AdvancedLearningService.conversion_alignment(model, application, posting)` scores a posting against the learned converting signature → zero callers, no router → `GET /api/criteria/{campaign_id}/alignment/{posting_id}` → results proxy → a per-row alignment chip in `applicantResults.js`/digest rows. Anchor: src/applicant/application/services/learning_advanced.py:281.
40. **Degraded-draft flag invisible in review** — [VALUE: med · EFFORT: S] — dark chain: `MaterialService.last_generation_degraded` + `silent_failure_count` (material_service.py:342) mark drafts that fell back to the deterministic template after LLM-ladder exhaustion → neither reaches any router, so the review UI presents a fallback draft as a real generation → include both in documents.py review/variant payloads → existing documents proxy → a "fallback draft — model was unavailable" badge in `documentLibrary.js`. Anchor: src/applicant/application/services/material_service.py:208.
41. **Cover-letter template merge-fill** — [VALUE: med · EFFORT: S] — dark chain: `MaterialService.fill_cover_letter_template(template, context)` deterministically fills `{{field}}` placeholders (no LLM, no fabrication risk) → zero callers, no router → `POST /api/documents/cover-letter/fill` → documents proxy → a template-library action in `documentLibrary.js`. Anchor: src/applicant/application/services/material_service.py:687.
42. **Bulk observation reconciliation into the attribute cloud** — [VALUE: med · EFFORT: M] — dark chain: `FeedbackService.ingest_parsed_input(campaign_id, parsed)` auto-applies non-integral values, stages integral ones for confirmation, surfaces conflicts, skips EEO — the "paste anything about yourself" importer — but feedback.py routes only freetext/survey → `POST /api/feedback/{campaign_id}/ingest` → memory proxy → a profile-import box in the Memory panel. Anchor: src/applicant/application/services/feedback_service.py:91.
43. **Pre-submit safety verdicts have no preview surface** — [VALUE: med · EFFORT: M] — dark chain: `check_scam_or_ghost_job` (:109), `check_duplicate_application` (:163), `check_per_company_volume_cap` (:226), `check_eligibility` (:353) raise `PresubmitBlock` only inside the loop; the user can never ask "would this posting be blocked, and why?" → a read-only `GET /api/outcomes/postings/{id}/presubmit-check` running the checks in report mode → results proxy → a safety-verdict tooltip on posting rows (see also §B7 item 61 for the silent-skip side). Anchor: src/applicant/application/services/presubmit_safety.py:109.
44. **Reflexion failure lessons: no producer, no viewer** — [VALUE: med · EFFORT: M] — dark chain: `LearningService.reflect_on_failure(failure)` distills a per-ATS `FailureLesson` and `recall_lessons(ats)` (learning_service.py:615) replays them — zero callers anywhere (the loop never reflects, nothing recalls, no router), and the store is an in-process dict → call from the loop's failure branches, persist via curated memory, expose `GET /api/admin/lessons/{ats}` → mind proxy → `applicantMind.js`. Anchor: src/applicant/application/services/learning_service.py:577.
45. **Per-ATS routine induction never runs** — [VALUE: med · EFFORT: M] — dark chain: `LearningService.induce_workflow(routine_store, domain, steps)` folds a successful pre-fill trace into a reusable per-ATS routine (data, not free text) → zero callers, so every ATS is re-derived cold → invoke after successful prefill in `agent_loop`, list induced routines via `GET /api/admin/routines` → mind proxy → a "learned procedures" panel (see §B7 item 68 for the store's volatility). Anchor: src/applicant/application/services/learning_service.py:557.
46. **ACE playbook delta engine orphaned; playbooks are chat-only** — [VALUE: med · EFFORT: M] — dark chain: `PlaybookService.apply_deltas`/`empty` implement auditable add/revise/retire playbook updates → zero callers; the only playbook path is the chat tools `save_playbook`/`update_playbook` (src/applicant/application/services/chat_tools.py:113/129 — agent-invocable, chat is the ONLY path) → wire the delta engine into the curation tick + `GET /api/agent-memory/playbooks/{ats}` → existing memory proxy → `applicantMind.js`. Anchor: src/applicant/application/services/playbook_service.py:64.
47. **Loop assisted-reasoning (`LOOP_TOOLS`) can never fire** — [VALUE: med · EFFORT: M] — dark chain: `AgentLoop.run_assisted_reasoning(...)` is the only consumer of `tools_for(campaign_id)` (agent_loop.py:437) and the container-wired `loop_toolset_factory` (container.py:1108), but nothing ever calls it — the opt-in autonomous tool-calling capability is dead regardless of the `LOOP_TOOLS` setting (§B8 item 94) → invoke from the tick decision points and surface its use in the run intent. Anchor: src/applicant/application/services/agent_loop.py:453.
48. **Configured LLM rate limiter never gates a call** — [VALUE: med · EFFORT: M] — dark chain: `CapacityService.admit_llm(call_id)`/`release_llm(call_id)` implement the per-provider LLM rate queue built from `LLM_RATE_*` (container.py:861), but both methods have zero callers → wrap the LLM adapter call path in admit/release; optionally expose queue occupancy in the agent-status payload. Anchor: src/applicant/application/services/capacity_service.py:88.
49. **Per-domain discovery pacing never applied** — [VALUE: med · EFFORT: M] — dark chain: `DiscoveryService.pace_schedule(postings)` spaces same-domain requests under the anti-bot interval → zero callers (`_source_pacer` is only touched inside this dead method), so bursts to one job board go unthrottled → call inside `run_discovery`; expose the effective interval in the sources listing. Anchor: src/applicant/application/services/discovery_service.py:223.
50. **Variant lineage walker unused** — [VALUE: low · EFFORT: S] — dark chain: `MaterialService.lineage(variant)` walks `parent_id` to the root; the routed variant library reads rows flat and never shows ancestry → include the chain in the variant-library response → existing proxy → an ancestry breadcrumb in `documentLibrary.js`. Anchor: src/applicant/application/services/material_service.py:660.
51. **Profile-gap report locked inside chat context** — [VALUE: low · EFFORT: S] — dark chain: `ChatService.identify_gaps(campaign_id)` lists missing core attributes + absent criteria, used only as hidden LLM context in `converse` (chat_service.py:1016) → `GET /api/setup/{campaign_id}/gaps` → setup proxy → a completeness checklist in onboarding/Settings. Anchor: src/applicant/application/services/chat_service.py:365.
52. **Self-improvement flywheel module entirely unwired** — [VALUE: low · EFFORT: L] — dark chain: `LearningFlywheel.tick` coordinating `AgentWorkflowMemory.record/relevant` (:73/:77), `AgentCognitiveEvolution.suggest_adjustments` (:113 — staged parameter tweaks for review) and `ReflexionEngine.reflect` (:151) is imported nowhere outside its own file — no container wiring, no tick, no router → wire backends, tick from the scheduler, surface staged `ParameterSuggestion`s as Portal pending actions. Anchor: src/applicant/application/services/learning_flywheel.py:183.

### B6 — Entity fields computed/stored but never rendered

53. **`FitScore.coverage` + `missing_terms` — the "why this variant" evidence** — [VALUE: med · EFFORT: S] — dark chain: per-variant JD keyword coverage (0..1) and the exact missing terms are computed and stored (src/applicant/core/entities/resume_variant.py:19-20) → grep finds zero renders of either in any workspace JS → show "covers 82% of the posting's language; missing: Kubernetes, SOC 2" on `documentLibrary.js` variant cards and the Debug variants tab. Anchor: src/applicant/core/entities/resume_variant.py:19.
54. **`Application.attributes_used` — "which of your details were used"** — [VALUE: med · EFFORT: S] — dark chain: every application records the attribute map it consumed (src/applicant/core/entities/application.py:35) → never serialized to any JS-rendered payload → a "data used on this application" disclosure on the review/tracker detail (a privacy-trust artifact the engine already keeps). Anchor: src/applicant/core/entities/application.py:35.
55. **`JobPosting.viability_score` + `rationale` outside the digest** — [VALUE: med · EFFORT: S] — dark chain: scoring writes `viability_score` and a structured `rationale` dict (src/applicant/core/entities/job_posting.py:26-27, via `ScoringService.score_viability`, scoring_service.py:93) → rendered only in the digest lane (`applicantDigest.js`); gallery rows, Results drill-downs, and history rows show no score/why → reuse the digest's rationale renderer on those surfaces. Anchor: src/applicant/core/entities/job_posting.py:26.
56. **Posting metadata (salary / location / work-mode) inconsistently surfaced** — [VALUE: med · EFFORT: S] — dark chain: `salary`, `location`, `work_mode`, `source_key` are captured per posting (src/applicant/core/entities/job_posting.py:19-23) → salary/work-mode appear in digest + criteria editor only; the gallery and any future tracker/history rows omit them → render the compensation/location line everywhere a posting appears. Anchor: src/applicant/core/entities/job_posting.py:21.
57. **`Application.sandbox_session_url` never rendered** — [VALUE: low · EFFORT: S] — dark chain: the application row stores its live sandbox session URL (src/applicant/core/entities/application.py:34) but no JS reads it — the remote lane rebuilds session lists from `/sessions` instead → a direct "watch this application" link on Portal/tracker rows. Anchor: src/applicant/core/entities/application.py:34.
58. **Plan-as-data artifacts (GOTO/FIND/FILL/… ops) never shown** — [VALUE: low · EFFORT: M] — dark chain: the planner emits typed op sequences (src/applicant/core/entities/plan.py:37-90) when `PREFILL_USE_PLANNER` is on, but the plan artifact is never persisted to a reviewable surface (screenshots/workflow steps are; the plan is not) → persist and show "the plan I followed" in the per-application detail. Anchor: src/applicant/core/entities/plan.py:37.
59. **`AgentIntent` history exists as an entity; only the latest sentence is shown** — [VALUE: low · EFFORT: S] — dark chain: intents persist with timestamps (src/applicant/core/entities/agent_intent.py:15-18); the strip shows only the latest (`applicantActivity.js`) → an intent timeline ("what it decided, when") in the Activity panel. Anchor: src/applicant/core/entities/agent_intent.py:15.

### B7 — Scheduler/tick and adapter behaviors invisible to the user

60. **Follow-up sender and ghosting sweep have no scheduler step** — [VALUE: high · EFFORT: M] — dark chain: even once §B2 wiring exists, nothing drives `send_scheduled_follow_ups`/`check_ghosting` on cadence — the scheduler has per-day steps for retention/curation (scheduler.py:470-484, :623) but none for post-submission → add a daily post-submission step alongside `_run_pii_retention`. Anchor: src/applicant/application/services/scheduler.py:470.
61. **Pre-submit safety blocks silently strand approved jobs** — [VALUE: high · EFFORT: M] — dark chain: G07 checks run every tick and skip the posting with only `log.info("presubmit_blocked")`, leaving it APPROVED forever with no user-visible reason → persist the block as a pending-action/feed item — "skipped 3 approved jobs: too old (1), duplicate within 30 days (2)" — with an override. Anchor: src/applicant/application/services/agent_loop.py:614.
62. **Given-up ("stuck") applications have no list and no reset** — [VALUE: high · EFFORT: M] — dark chain: after 5 failed resumes an app enters the process-lived `ResumeLedger` give-up set and is excluded from all future re-drives; one deduped notification fires, but no endpoint lists given-up apps or clears the flag (only a process restart does) → a "paused applications" panel with per-app failure count and a Retry button that clears the ledger entry. Anchor: src/applicant/application/services/agent_loop.py:713.
63. **Loop-health metrics are delivered to the browser and never rendered** — [VALUE: high · EFFORT: S] — dark chain: agent status packs `scheduler.state()` (tick totals, success/failure, consecutive-failure count, stall-alert armed — scheduler.py:358) into the payload and the activity snapshot proxies it, but `applicantActivity.js:168` reads only `scheduler.running` → a health chip: "312 runs · last failure 2d ago" plus a visible stall warning. Anchor: src/applicant/app/routers/agent_status.py:164.
64. **Per-tick "why nothing happened" reasons never reach the user** — [VALUE: high · EFFORT: M] — dark chain: `TickResult.reason` (run_mode_stop, automated_work_gated, budget_exhausted, …) exists per tick, but a gated tick returns before `_record_intent`, so scheduled runs leave no persisted trace (only manual Run-now returns the payload) → persist the reason on agent_runs and show "last run: skipped — waiting on setup" in the status strip. Anchor: src/applicant/application/services/agent_loop.py:414.
65. **Live vs. offline discovery is indistinguishable in the UI** — [VALUE: high · EFFORT: S] — dark chain: with `DISCOVERY_LIVE=false` every board is a fake offline client with an identical source registry (a SampleSource emits `example.test` rows), so the Sources tab looks the same whether real boards are queried or synthetic data flows into digests → a live/offline badge per source + a sample-data warning. Anchor: src/applicant/adapters/discovery/factory.py:44.
66. **Curation nudge ships permanently dormant while its review UI waits empty** — [VALUE: high · EFFORT: S] — dark chain: the Mind curation queue (agent_memory.py:189 → `applicantMind.js`) is fully surfaced, but the scheduler step that generates proposals is gated on `CURATION_SCHEDULE` default "off" (env-only, §B8 item 100) → a Settings toggle "let the agent propose memory/skill updates daily". Anchor: src/applicant/app/config.py:302.
67. **Captcha solving wired with zero status surface** — [VALUE: high · EFFORT: M] — dark chain: the solver (avoidance leg + third-party token solver) is built and injected into every `PrefillService` (container.py:884), but no router reports the active strategy and solve/avoid/handoff outcomes are never shown (strategy knob itself: §B8 item 83) → a Settings card with strategy, key status, and solved/avoided/handed-off counters. Anchor: src/applicant/app/container.py:884.
68. **Induced per-ATS routines are dark and volatile** — [VALUE: med · EFFORT: M] — dark chain: the process-lived `InMemoryRoutineStore` accumulates induced form-fill routines plus success/failure scores and injects them as planning priors, but no router or UI lists routines/scores — and everything silently resets on restart → a "learned site routines" table (domain, steps, win rate) with a forget button, backed by persistence. Anchor: src/applicant/app/container.py:894.
69. **Engine never writes the interview calendar events the workspace already accepts** — [VALUE: med · EFFORT: M] — dark chain: the workspace implements token-gated `POST /api/applicant/internal/calendar/events` (workspace/routes/applicant_internal_routes.py:744), but `HttpWorkspaceClient` has no create-event method and no engine code calls it — detected interviews are read (lane A) yet never written back → a client method + a post-submission "interview detected → add to calendar" action. Anchor: workspace/routes/applicant_internal_routes.py:744.
70. **Local-models callback is a dead endpoint** — [VALUE: med · EFFORT: M] — dark chain: the workspace serves `GET /api/applicant/internal/local-models` (workspace/routes/applicant_internal_routes.py:919), but no engine client method or caller exists — the model-connect lane can never auto-discover the workspace's local models → wire into `ModelEndpointService` as a suggested-endpoint source in the connect-a-model UI. Anchor: workspace/routes/applicant_internal_routes.py:919.
71. **Engine↔workspace bridge health is unobservable** — [VALUE: med · EFFORT: S] — dark chain: `HttpWorkspaceClient.ping` exists exactly for a health probe, but nothing calls it and no surface shows whether `APPLICANT_INTERNAL_TOKEN` is configured/valid — a bad token silently disables calendar, deep-research, and the memory bridge → a Settings "workspace link" status row backed by a ping-through endpoint. Anchor: src/applicant/adapters/workspace/http_workspace_client.py:144.
72. **Sandbox capacity pacing is invisible** — [VALUE: med · EFFORT: M] — dark chain: `CapacityService` admits/defers/yields sandbox slots every tick (capacity_service.py:60-83); deferred admissions emit only `log.info("sandbox_admission_deferred")` and no router exposes slots-in-use or waiting apps → an Activity line "2 applications waiting for a browser slot" from a capacity snapshot endpoint. Anchor: src/applicant/application/services/agent_loop.py:639.
73. **Per-campaign tick failures and overlap-skips are log-only** — [VALUE: med · EFFORT: S] — dark chain: `campaign_tick_failed` (with the error) and `campaign_tick_skipped_in_progress` happen inside every tick but never reach storage; the metrics snapshot counts only whole-tick failures → a per-campaign "last run error / skipped (still busy)" field on the run-status payload the status chip already reads. Anchor: src/applicant/application/services/scheduler.py:221.
74. **Smart LLM router is ON by default with zero status surface** — [VALUE: med · EFFORT: S] — dark chain: the smart router silently reorders the tier ladder every resolve; the container comment says "exposed for status/health" but no router reads `container.llm_router` — users cannot see which endpoint served a task or why → a routing row in the model-ladder UI (chosen tier, reason, local-preference state). Anchor: src/applicant/app/container.py:582.
75. **Per-run stats persist every tick and the JS drops them** — [VALUE: med · EFFORT: S] — dark chain: every tick durably records discovered/digest_rows/pipelines_started/handoffs/completed/budget_remaining into agent_runs (agent_loop.py:1326); the ops proxy returns the run list, but the Run-controls tab uses `runs.items[0]` only for config defaults → a "recent runs" mini-table (time, found, started, handed off). Anchor: src/applicant/application/services/agent_loop.py:1326.
76. **Company-research provenance is lost per application** — [VALUE: med · EFFORT: M] — dark chain: the loop auto-escalates to capped deep research on a knowledge gap and marks `summary["research_used"]` — but the flag lives only in the orchestrator checkpoint; the material-review UI never shows that (or which) research informed a letter → a "research used" badge + findings excerpt on the redline review card. Anchor: src/applicant/application/services/agent_loop.py:980.
77. **Notification escalation-ladder state (held → email) is invisible** — [VALUE: med · EFFORT: M] — dark chain: every tick advances the hold→email escalation ladder and quiet-hours holds (scheduler.py:262), but nothing shows which notifications are held on a rung or when escalation fires → an inbox annotation "held — escalates to email at HH:MM". Anchor: src/applicant/application/services/scheduler.py:262.
78. **Resume-backoff countdown invisible on blocked cards** — [VALUE: low · EFFORT: S] — dark chain: each parked application is re-driven at most every 300s via the process-lived `ResumeLedger` (agent_loop.py:264), so after the user clears a blocker the app may sit up to 5 minutes with no sign anything will happen → "retrying at HH:MM:SS" on the blocked-application card (last-resume + backoff already known). Anchor: src/applicant/application/services/agent_loop.py:264.
79. **Embedding backend (hash-trick) quality is undisclosed** — [VALUE: low · EFFORT: M] — dark chain: `LocalEmbedding` is a deterministic hashing embedding powering dedup, variant scoring, and signature learning; no surface reveals the backend or offers the documented real-model swap, so semantic-match quality is silently the fallback → a Settings/debug row "matching engine: basic (offline) / model-backed". Anchor: src/applicant/adapters/embedding/local_embedding.py:55.
80. **RSS discovery sources are hardcoded with no add-feed UI** — [VALUE: low · EFFORT: M] — dark chain: the discovery registry is extensible per source key and the toggle UI exists, but `RSS_FEEDS` is a hardcoded one-entry dict — users cannot add a niche job feed without a code change → an "add feed URL" control on the Sources tab persisting into the registry. Anchor: src/applicant/adapters/discovery/factory.py:39.
81. **"Run now" implies a digest refresh the DigestLedger suppresses** — [VALUE: low · EFFORT: S] — dark chain: Run-now copy suggests an immediate digest, but force ticks still honor the once-per-day `digest_sent` guard (agent_loop.py:527, `DigestLedger` agent_loop.py:86) and no surface shows "today's digest already delivered at HH:MM" → a digest-status line next to the Run-now button. Anchor: src/applicant/application/services/agent_loop.py:527.

### B8 — Config knobs with no Settings UI (env-only capability)

The workspace Settings surface mounts only four wizard renderers (`channels`, `sandbox`, `fonts`,
`update`) plus the campaign and model-ladder tabs — there is no generic engine-preferences tab, so
every knob below is unreachable short of editing the compose file. Several are not even plumbed into
`docker/docker-compose.prod.yml`.

82. **Browser timezone/locale pinned to Arizona for everyone** — [VALUE: high · EFFORT: S] — dark chain: `EGRESS_TIMEZONE` default `America/Phoenix` + `EGRESS_LOCALE` `en-US` thread into the browser context so the fingerprint matches the exit IP → no Settings UI, no setup endpoint, not in compose env — every non-Arizona user runs a timezone/IP contradiction, the exact tell the stealth layer exists to prevent → Settings > Automation (or auto-derive from egress IP with a visible override). Anchor: src/applicant/app/config.py:469.
83. **Captcha handling strategy is env-only** — [VALUE: high · EFFORT: M] — dark chain: `CAPTCHA_STRATEGY` default `human` (+ `CAPTCHA_SERVICE`, `CAPTCHA_API_KEY`) controls whether captchas hand off to the human, are avoided, or are solved by a paid third-party service → a consequential, terms-sensitive opt-in with no in-product consent surface → Settings > Automation with a plain-language warning + vaulted key entry (status surface: §B7 item 67). Anchor: src/applicant/app/config.py:425.
84. **Automated account creation opt-in** — [VALUE: high · EFFORT: S] — dark chain: `ALLOW_AUTOMATED_ACCOUNTS` default False lets the engine create ATS accounts from vaulted credentials instead of handing off → no Settings UI, no setup endpoint, not in compose env → Settings > Automation toggle (gate stays server-side). Anchor: src/applicant/app/config.py:411.
85. **Per-company daily application cap** — [VALUE: high · EFFORT: S] — dark chain: `PRESUBMIT_MAX_APPS_PER_COMPANY_PER_DAY` default 3 → no UI anywhere → Settings > Campaign next to the existing daily-target field (its silent enforcement is §B7 item 61). Anchor: src/applicant/app/config.py:565.
86. **Scheduler tick cadence** — [VALUE: high · EFFORT: S] — dark chain: `SCHEDULER_INTERVAL_SECONDS` default 60.0 sets how often the 24/7 loop ticks → no UI, not in compose (only `SCHEDULER_ENABLED` passes through) → Settings > Automation cadence control. Anchor: src/applicant/app/config.py:224.
87. **PII retention window + sweep schedule** — [VALUE: high · EFFORT: M] — dark chain: `PII_RETENTION_DAYS` default 0 (keep forever) + `PII_RETENTION_SCHEDULE` default `off` govern how long parsed PII/EEO/intake is kept → no UI; the product keeps everything indefinitely by default with no way to change that → Settings > Privacy (manual sweep + report: §B5 item 37). Anchor: src/applicant/app/config.py:192.
88. **Duplicate-application cooldown** — [VALUE: high · EFFORT: S] — dark chain: `PRESUBMIT_DUPLICATE_COOLDOWN_DAYS` default 30 sets how long before re-applying to the same company/role → no UI → Settings > Campaign pacing. Anchor: src/applicant/app/config.py:561.
89. **Residential egress mode + proxy attestation** — [VALUE: high · EFFORT: M] — dark chain: `EGRESS_MODE` default `direct`, `EGRESS_PROXY_URL`, `EGRESS_RESIDENTIAL` default False decide whether automation exits via a residential proxy (datacenter exit refused) → no UI, not in compose (compose passes only the separate `EGRESS_PROXY` for the captcha solver) → Settings > Automation network section. Anchor: src/applicant/app/config.py:363.
90. **Approval-gate timeout window** — [VALUE: med · EFFORT: S] — dark chain: `APPROVAL_TIMEOUT_DAYS` default 30 (+ `APPROVAL_WAIT_SECONDS`) sets how long a pending final-approval waits before timing out → no UI → Settings > Approvals ("cancel unapproved applications after N days"). Anchor: src/applicant/app/config.py:207.
91. **Universal-ATS fill-rate floor** — [VALUE: med · EFFORT: S] — dark chain: `ATS_MATCH_RATE_FLOOR` default 0.2 is the minimum fields-filled ratio below which an application is flagged for review instead of offered for submit → no UI → Settings > Automation quality threshold. Anchor: src/applicant/app/config.py:402.
92. **Sandbox backend + stealth persona selection** — [VALUE: med · EFFORT: M] — dark chain: `SANDBOX_BACKEND` default `local` and `STEALTH_PERSONA` choose local vs remote-VM sandbox and the fingerprint persona → the connection form exists in setup, but the backend/persona switch has no UI → Settings > Automation sandbox selector beside the existing connection form. Anchor: src/applicant/app/config.py:495.
93. **Browser engine choice** — [VALUE: med · EFFORT: S] — dark chain: `BROWSER_ENGINE` (anti-detect default vs `chromium` + `BROWSER_CHANNEL`) selects the browser ALL outbound automation routes through → in compose but no Settings UI → Settings > Automation advanced. Anchor: src/applicant/app/config.py:377.
94. **Assistant/loop tool autonomy master switches** — [VALUE: med · EFFORT: M] — dark chain: `CHAT_TOOLS` and `LOOP_TOOLS` default `off` gate whether the assistant/loop may call tools mid-reasoning; neither is in compose (unreachable in deploy) and the per-tool Debug registry sits below these invisible master switches → an "agent tools" enable row on the Tools tab (loop side is also dead code: §B5 item 47). Anchor: src/applicant/app/config.py:324.
95. **Company-research enrichment for cover letters** — [VALUE: med · EFFORT: S] — dark chain: `MATERIAL_RESEARCH_ENABLED` default False folds capped deep-research facts into letters → in compose but no Settings UI → Settings > Documents next to aggressiveness/banned-phrases. Anchor: src/applicant/app/config.py:279.
96. **Desktop-assist backend + approval posture** — [VALUE: med · EFFORT: M] — dark chain: `COMPUTER_USE_BACKEND` default `noop`, `COMPUTER_USE_MODE`, `COMPUTER_USE_APPROVALS` (`manual` per-action vs per-session) → in compose but no Settings UI despite a `desktop_assist` section in the feature registry → Settings > Automation desktop-assist panel. Anchor: src/applicant/app/config.py:443.
97. **Pre-submit eligibility (work-auth) filter toggle** — [VALUE: med · EFFORT: S] — dark chain: `PRESUBMIT_ELIGIBILITY_ENABLED` default True filters postings on work-authorization/sponsorship/clearance → no UI to relax it → Settings > Campaign filters. Anchor: src/applicant/app/config.py:571.
98. **Scam/ghost-job max listing age** — [VALUE: med · EFFORT: S] — dark chain: `PRESUBMIT_MAX_LISTING_AGE_DAYS` default 90 blocks older postings → no UI → Settings > Campaign discovery filters. Anchor: src/applicant/app/config.py:556.
99. **Memory/skills write-approval + memory size caps** — [VALUE: med · EFFORT: M] — dark chain: `MEMORY_WRITE_APPROVAL`/`SKILLS_WRITE_APPROVAL` default True and `MEMORY_MAX_CHARS`/`USER_MAX_CHARS` (8000/4000) govern auto-apply vs staged memory writes and the prompt budget → no UI in the existing memory panel → Settings > Assistant memory. Anchor: src/applicant/app/config.py:293.
100. **Proactive nudge / status / curation cadences** — [VALUE: med · EFFORT: S] — dark chain: `CURATION_SCHEDULE` (`off`), `STATUS_UPDATE_SCHEDULE` (`off`), `ESSENTIALS_NUDGE_SCHEDULE` (`daily`) control the daily pushes ("still blocked", "where campaigns stand", memory curation) — two ship dormant, one ships on, none is controllable → Settings > Notifications cadence toggles (the dormant curation queue is §B7 item 66; the status push is `StatusUpdateService.emit`, status_update.py:59). Anchor: src/applicant/app/config.py:302.
101. **Discovery proxy list** — [VALUE: med · EFFORT: S] — dark chain: `DISCOVERY_PROXIES` empty (direct egress) is the proxy hook for job-board discovery → no UI → Settings > Automation network, companion to item 89. Anchor: src/applicant/app/config.py:357.
102. **Smart LLM routing + prefer-local policy** — [VALUE: med · EFFORT: S] — dark chain: `LLM_SMART_ROUTING` default True and `LLM_SMART_ROUTING_PREFER_LOCAL` True silently reorder the tier ladder → the ladder UI exists but not this policy toggle → Settings > AI beside the ladder (status surface: §B7 item 74). Anchor: src/applicant/app/config.py:546.
103. **Takeover desktop environment + remote-view backend** — [VALUE: low · EFFORT: S] — dark chain: `TAKEOVER_DESKTOP` (desktop environment) and `REMOTE_VIEW_BACKEND` choose the live-takeover UX → only the image name is in compose → Settings > Automation takeover appearance. Anchor: src/applicant/app/config.py:478.
104. **Resume render fidelity** — [VALUE: low · EFFORT: S] — dark chain: `RESUME_RENDER` default `auto` forces/disables the real LaTeX+LibreOffice render vs deterministic stub → no UI → Settings > Documents ("force high-fidelity render"). Anchor: src/applicant/app/config.py:350.
105. **Context compression threshold + prefix cache** — [VALUE: low · EFFORT: S] — dark chain: `CONTEXT_COMPRESS_THRESHOLD` default 64000 (0 disables) and `PREFIX_CACHE` `auto` tune token budget/caching → Settings > AI advanced (borderline operator). Anchor: src/applicant/app/config.py:179.
106. **Loop failure-alert threshold + queue/rate limits** — [VALUE: low · EFFORT: S] — dark chain: `LOOP_FAILURE_ALERT_THRESHOLD` default 3, `SANDBOX_CONCURRENCY` 3, `LLM_RATE_LIMIT`/`LLM_RATE_PERIOD` (30/60s) tune alerting and throughput (the LLM limiter is also unenforced: §B5 item 48) → expose at most the alert threshold in Settings > Notifications. Anchor: src/applicant/app/config.py:233.
107. **Plan-as-data prefill planner flag** — [VALUE: low · EFFORT: S] — dark chain: `PREFILL_USE_PLANNER` default False switches pre-fill to the experimental planner → env-only → Settings > Automation advanced flag (plan visibility: §B6 item 58). Anchor: src/applicant/app/config.py:532.
108. **Operator/deploy-only knobs (grouped — correctly env-only, listed for completeness)** — [VALUE: low · EFFORT: —] — `APPLICANT_MODE`, `DATABASE_URL`, `APP_STATIC_DIR`, `CREDENTIAL_KEYFILE`, `ORCHESTRATOR_BACKEND`/`CHECKPOINT_DIR`, `FONTS_DIR`/`BROWSER_PROFILES_DIR`, `SCHEDULER_ENABLED`, `DISCOVERY_LIVE`/`NOTIFICATIONS_LIVE`/`BROWSER_REAL` (production preset flags), `SEARXNG_URL`, `WORKSPACE_URL`+`APPLICANT_INTERNAL_TOKEN`, boot-default channel URLs (the channels UI owns these at runtime), `LOG_FORMAT`/`LOG_LEVEL`, `MIND_BACKEND`, `CURATION_MODEL`, `CUA_DRIVER_CMD`/`CUA_TELEMETRY`, `EGRESS_PROXY` (captcha exit), `TAKEOVER_DESKTOP_IMAGE`/`_BASE_URL`, and the `PROXMOX_*` block (already collected at runtime via the setup sandbox-connection endpoint). Anchors: src/applicant/app/config.py:158-526.

---

## C. Synthesis

- **108 dark or half-wired chains** across six dimensions: 5 broken/dead wiring, 27 fully dark
  no-router capabilities (§B2+§B5), 9 endpoint gaps with no proxy (§B3), 8 admin-gated/JS-orphaned
  (§B4), 7 unrendered entity-field groups (§B6), 22 invisible scheduler/adapter behaviors (§B7), and
  27 env-only config knobs (§B8).
- **The single deepest failure is still the post-submission lifecycle** — but the new finding is *how*
  dark it is: tables migrated, service built, injected into the submission path, and then never called
  once (§B2). Wiring `enter_post_submission` into the existing final-submit handlers plus promoting
  the already-proxied `admin/history` payload (item 25) yields a working tracker board in days, not
  weeks.
- **A recurring "last-mile" pattern**: the engine has a well-built read-model spine
  (`scheduler.state()`, run stats, diagnostics rings, detections, stealth posture) that already reaches
  the workspace proxy — and dies there for want of ~20 lines of JS each (items 26, 27, 63, 75).
- **Three safety/spec features are dead code, not just dark**: emergency copy/paste handoff (35),
  loop tool-calling (47), and the LLM rate limiter (48) — plus the ghosting sweep's age bug (9).
  These belong in the next green increment regardless of UI priorities.
- **Settings is the biggest single structural gap**: there is no engine-preferences surface at all, so
  every §B8 knob — including safety-relevant ones (82, 83, 84, 87, 89) — is invisible and several are
  not even plumbed to deploy.
